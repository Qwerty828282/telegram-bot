[build]
builder = "NIXPACKS"
buildCommand = "echo 'Python bot — no build step'"

[deploy]
startCommand = "/opt/venv/bin/python bot/bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
